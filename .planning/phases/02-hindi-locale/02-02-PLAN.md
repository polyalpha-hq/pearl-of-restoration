---
phase: 02-hindi-locale
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - index.html
autonomous: false
requirements:
  - I18N-01
  - I18N-03

must_haves:
  truths:
    - "A visitor sees a HI button in the nav language switcher alongside EN, ES, and PT"
    - "Clicking HI renders the full page in Hindi without a page reload"
    - "After selecting HI, refreshing the browser reloads the page in Hindi (localStorage persistence)"
    - "The HI button highlights (gets the active CSS class) when Hindi is the current locale"
  artifacts:
    - path: "index.html"
      provides: "HI button in .lang-sw div"
      contains: "data-lang=\"hi\""
  key_links:
    - from: ".lang-sw HI button"
      to: "setLang('hi')"
      via: "onclick attribute"
      pattern: "onclick=\"setLang\\('hi'\\)\""
    - from: "setLang('hi')"
      to: "i18n.hi"
      via: "i18n[lang] lookup"
      pattern: "hi:\\{"
---

<objective>
Add the HI language toggle button to the nav's `.lang-sw` div in `index.html`.

Purpose: Expose the Hindi locale (added in Plan 02-01) to visitors via the language switcher. The `setLang` function and `i18n.hi` object already exist after Plan 02-01 — this plan adds only the button HTML and performs a human visual verification checkpoint.

Output: `index.html` nav contains four language buttons (EN, ES, PT, HI). The HI button is wired to `setLang('hi')` and carries `data-lang="hi"` so the active-state toggle CSS works correctly.
</objective>

<execution_context>
@/Users/votykvot/Desktop/pearl-of-restoration/.planning/phases/02-hindi-locale/../../../../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/votykvot/Desktop/pearl-of-restoration/.planning/ROADMAP.md
@/Users/votykvot/Desktop/pearl-of-restoration/CLAUDE.md
@/Users/votykvot/Desktop/pearl-of-restoration/.planning/phases/02-hindi-locale/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HI button to .lang-sw div in nav</name>

  <read_first>
    Read index.html line 298 to confirm the current `.lang-sw` div content before editing. It currently reads:
    `&lt;div class="lang-sw"&gt;&lt;button class="lang-btn" onclick="setLang('en')" data-lang="en"&gt;EN&lt;/button&gt;&lt;button class="lang-btn" onclick="setLang('es')" data-lang="es"&gt;ES&lt;/button&gt;&lt;button class="lang-btn" onclick="setLang('pt')" data-lang="pt"&gt;PT&lt;/button&gt;&lt;/div&gt;`
    Verify this exact text exists before making any edit.
  </read_first>

  <action>
    Using the Edit tool, locate the exact string on line 298:

      `<button class="lang-btn" onclick="setLang('pt')" data-lang="pt">PT</button></div>`

    Replace it with:

      `<button class="lang-btn" onclick="setLang('pt')" data-lang="pt">PT</button><button class="lang-btn" onclick="setLang('hi')" data-lang="hi">HI</button></div>`

    No other changes to this line. The full `.lang-sw` div after the edit must be:
    `<div class="lang-sw"><button class="lang-btn" onclick="setLang('en')" data-lang="en">EN</button><button class="lang-btn" onclick="setLang('es')" data-lang="es">ES</button><button class="lang-btn" onclick="setLang('pt')" data-lang="pt">PT</button><button class="lang-btn" onclick="setLang('hi')" data-lang="hi">HI</button></div>`

    No JavaScript changes required. The existing setLang function already:
    - resolves i18n['hi'] (added in 02-01)
    - calls `b.classList.toggle('active', b.getAttribute('data-lang')===lang)` across all .lang-btn elements, so the new HI button will receive the active class automatically when Hindi is selected
  </action>

  <verify>
    <automated>grep -c "onclick=\"setLang('hi')\" data-lang=\"hi\"" /Users/votykvot/Desktop/pearl-of-restoration/index.html</automated>
    Expected output: 1

    <automated>grep "lang-sw" /Users/votykvot/Desktop/pearl-of-restoration/index.html</automated>
    Expected: output contains both `data-lang="pt"` and `data-lang="hi"` on the same line, in that order (PT before HI)
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -c 'onclick="setLang('"'"'hi'"'"')" data-lang="hi"' index.html` returns 1
    - Source assertion: The HI button appears after the PT button (same line, PT button text then HI button text in document order)
    - Source assertion: The closing `</div>` of `.lang-sw` comes immediately after the HI button — no extra whitespace or stray tags inserted
    - Behavior assertion: No syntax error introduced — the surrounding HTML is valid (the lang-sw div closes correctly)
  </acceptance_criteria>

  <done>
    index.html nav `.lang-sw` div contains four buttons: EN, ES, PT, HI in that order.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — HI locale renders correctly in browser</name>

  <what-built>
    Plan 02-01 added hi:{} with ~80 Hindi translations to the i18n const. Plan 02-02 Task 1 added the HI button to the nav. The full Hindi locale is now wired end-to-end.
  </what-built>

  <how-to-verify>
    1. Start local server (from the project root): `npx serve /Users/votykvot/Desktop/pearl-of-restoration`
    2. Open the URL shown by `npx serve` in a browser (typically http://localhost:3000 or http://localhost:5000)
    3. Confirm four language buttons appear in the nav: EN, ES, PT, HI
    4. Click HI — verify the entire page switches to Hindi (Devanagari script) without a page reload. Spot-check at minimum:
       - Nav links are in Hindi (e.g., "हमारे बारे में")
       - Hero title is in Hindi
       - Crisis section title is in Hindi
       - Footer links are in Hindi
       - Donate button in nav is in Hindi ("दान करें")
    5. Confirm no English strings remain visible in any section while HI is active
    6. Close the browser tab (or open a new private/incognito window) and navigate back to the page — confirm it loads in Hindi (localStorage persistence)
    7. Switch back to EN — confirm the page returns to English correctly
    8. Verify that switching EN → ES → PT → HI → EN cycles all work without errors in the browser console
  </how-to-verify>

  <resume-signal>Type "approved" if all verification steps pass, or describe any issues found (e.g., "key X not translating", "button order wrong", "console error on switch")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nav onclick | Button onclick="setLang('hi')" — string literal, not user-supplied input |
| localStorage | pearl_lang written by setLang, read on page load — locale code only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | lang-btn onclick attribute | accept | onclick value is a static string literal in committed source; no user input path exists to modify it at runtime. GitHub Pages serves only committed content. |
| T-02-05 | Information Disclosure | localStorage pearl_lang | accept | Inherited from T-02-02 in Plan 02-01; no new exposure. Value is a locale code only. |
</threat_model>

<verification>
1. `grep -c 'data-lang="hi"' index.html` returns 1 — exactly one HI button
2. `grep "lang-sw" index.html` output shows PT button before HI button on the same line
3. Browser visual check: HI button appears in nav, clicking it renders full Devanagari page
4. localStorage check: after clicking HI, `localStorage.getItem('pearl_lang')` in browser console returns 'hi'
5. Refresh check: page reloads in Hindi after selecting HI and refreshing
</verification>

<success_criteria>
- Four language buttons (EN, ES, PT, HI) visible in nav on all screen sizes
- Clicking HI renders every data-i18n element in Hindi with no English strings visible
- Devanagari text renders via system font fallback (no broken-square glyph boxes for common browsers)
- Active state CSS highlights HI button when Hindi is current locale
- localStorage persists locale selection across page reload
- Switching between all four locales works correctly in both directions
</success_criteria>

<output>
After completion, create `/Users/votykvot/Desktop/pearl-of-restoration/.planning/phases/02-hindi-locale/02-02-SUMMARY.md` using the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
